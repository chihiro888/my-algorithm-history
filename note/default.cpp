#include <iostream>
#include <string>
#include <stack>
#include <vector>

#define FOR(i, start, end) for(int i = start; i < end; ++i)
#define REP(i,n) FOR(i,0,n)

using namespace std;

int main() {
    // TODO
    return 0;
}